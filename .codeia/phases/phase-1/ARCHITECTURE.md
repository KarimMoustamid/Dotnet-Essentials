# Phase 1 Architecture — Backend Domain Models

Phase 1 defines the two core domain entities, Game and Genre, inside the GameStore.Api project (which is part of the Backend solution). The Game entity holds a foreign key GenreId that links to the Genre entity, establishing the foundational data relationship for the game store.

```mermaid
flowchart TD
  subgraph BackendSolution["Backend Solution"]
    SolutionFile["Backend/Backend.sln"]
    ProjectFile["src/GameStore.Api/GameStore.Api.csproj"]
  end
  SolutionFile -->|references| ProjectFile
  subgraph ModelsNamespace["Models (GameStore.Api.Models)"]
    GameEntity["src/GameStore.Api/Models/Game.cs<br/>Game entity"]
    GenreEntity["src/GameStore.Api/Models/Genre.cs<br/>Genre entity"]
  end
  ProjectFile -->|contains| ModelsNamespace
  GameEntity -->|GenreId (FK)| GenreEntity
```

---

*Generated by Codeia — re-run **Codeia: Generate Phase Diagram** to regenerate*
